{
  "name": "hk-pmtiles-server",
  "version": "1.0.0",
  "description": "PMTiles tile server for parcels and buildings",
  "main": "server.js",
  "scripts": {
    "postinstall": "./fetch-lfs-files.sh || echo 'LFS fetch skipped'",
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "pmtiles": "^3.0.7"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
