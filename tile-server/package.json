{
  "name": "hk-pmtiles-server",
  "version": "1.0.0",
  "description": "PMTiles tile server for parcels and buildings",
  "main": "server.js",
  "scripts": {
    "postinstall": "./download-pmtiles.sh || ./fetch-lfs-files.sh || echo 'PMTiles download skipped - files should already exist'",
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "pmtiles": "^3.0.7"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
